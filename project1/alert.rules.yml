groups:
  - name: flask-alerts
    rules:
      - alert: AppDown
        expr: up == 0
        for: 10s
        labels:
          severity: critical
        annotations:
          summary: "flask app is down"
          description: "flask application is not responding"
